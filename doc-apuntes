Instalar express: npm i nodemon -D

Para ejecutarlo: npm run start.+

/*



*/

/*

GET: para c¡tener informacion.

PUT: Modificar los elementos

*/

/*

resolve: si no hay errores los resuelve

reject: rechaza si hay algun error

*/

/*

find devuelve el primer elemento que lo cumpla

*/

Set es un objeto que colecciona valores

*/

*/

const [pokedex, setPokedex] useState([])

const: constante que fija una variable durante doto el scrypt. 
pokedex: la variable que en este caso fija la constante.
setPokedex: la funcion que actualiza el valor de la variable.
useState: el hook.
([]): el array del hook muestra el valor inicial de la variable

*/

*/

const [variable, setVariable] = useState()

variable = es la constante con el valor inicial.
setVariable = es la funcion que se encarga de actualizar el valor inicial de la variable
useState() = es el hook que da un valor predeterminado a la variable

/*
